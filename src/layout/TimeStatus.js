import React from 'react';
import styles from '../styles/layout/TopStatus.module.scss'

const TimeStatus = () => {
    return (
        <div>
            time
        </div>
    );
};

export default TimeStatus;