import React from 'react';
import styles from '../styles/pages/ChatList.module.scss'

const ChatList = () => {
    return (
        <div>
            chatList
        </div>
    );
};

export default ChatList;